---
/**
 * ResponseExample - Wrapper for API response code examples in MDX
 * Content inside is relocated to the right panel by the API layout's client script.
 *
 * Usage in MDX:
 * <ResponseExample>
 * ```json title="200"
 * { "id": 1, "name": "Leanne Graham" }
 * ```
 * ```json title="404"
 * { "error": "User not found" }
 * ```
 * </ResponseExample>
 */
---

<div data-api-panel="response" class="api-panel-source">
  <slot />
</div>

<style>
  @reference "../../styles/global.css";

  /* Hidden in the left column — relocated to right panel by JS */
  .api-panel-source {
    display: none;
  }
</style>
